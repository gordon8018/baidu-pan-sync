[project]
name = "baidu-pan-sync"
version = "0.1.0"
description = "Incremental Baidu Netdisk share synchronizer with a SQLite ledger"
requires-python = ">=3.11"

[project.scripts]
baidu-pan-sync = "baidu_pan_sync.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]

